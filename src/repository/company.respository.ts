import { Company } from "@prisma/client";
import { CompanyAttributes } from "../types/company.types";

class CompanyRepository {
   // async registerCompany(companyData: Omit<CompanyAttributes, 'id'>): Promise<Company> {
   //    const {}
   // }
}

export default new CompanyRepository();